# Tools

`provider_smoke_tests/` contains small connectivity checks for model endpoints. They read credentials from environment variables and should never contain committed keys. Provider smoke tests establish connectivity only; they are not benchmark evaluations and should not be used as evidence of skill quality.

