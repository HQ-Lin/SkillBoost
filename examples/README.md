# Seed skills

`seed-skills/` contains initial procedural artifacts used to start evolution runs. They are examples of task skills, not part of the SkillBoost control loop and not necessarily state-of-the-art solutions.

A seed skill should be versioned or content-addressed before evaluation. Evolved descendants must retain the seed checksum in their lineage records so improvements can be attributed to a fixed starting point.

