# Analysis

Analysis scripts generate evaluation summaries, token statistics, convergence plots, structure ablations, skill-length trends, and cross-model comparisons.

For publication-quality figures, run scripts from the project root with explicit input/output arguments where supported. Treat hard-coded paths and plotting defaults as experiment snapshots; do not infer a result from a filename alone. Statistical claims should use paired case-level data and disclose all attempted evolution rounds.

